{
  "name": "alhikmahschoolwellbeing",
  "version": "2.2.0",
  "private": true,
  "description": "Cloudflare Worker student wellbeing platform backed by Neon PostgreSQL",
  "type": "module",
  "scripts": {
    "worker:build": "node scripts/build-worker.js",
    "worker:dev": "npm run worker:build && wrangler dev",
    "worker:deploy": "npm run worker:build && wrangler deploy",
    "worker:secret:database": "wrangler secret put DATABASE_URL",
    "migrate": "node --env-file=.env.local scripts/migrate.js",
    "check": "npm run worker:build && node --check worker/index.js && node --check worker/index.built.js && node --check tests/worker.test.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
