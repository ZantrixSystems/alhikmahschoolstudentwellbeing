{
  "name": "alhikmahschoolwellbeing",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker wellbeing platform backed by Neon PostgreSQL",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "node --env-file=.env.local scripts/migrate.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.74.0"
  }
}
